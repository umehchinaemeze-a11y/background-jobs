# Test environment — points at the isolated test database on the same server.
DATABASE_URL="postgresql://jobs:jobs@localhost:15435/jobs_test?schema=public"

JOB_MAX_ATTEMPTS=5
JOB_WORKER_CONCURRENCY=4
JOB_RETRY_BASE_DELAY_MS=500
JOB_RETRY_JITTER_MS=200
JOB_RETRY_MAX_DELAY_MS=10000
JOB_STUCK_TIMEOUT_MS=2000
JOB_POLL_INTERVAL_MS=200
JOB_WORK_TIMEOUT_MS=5000

EMAIL_PROVIDER_VENDOR=test
EMAIL_PROVIDER_API_KEY=
EMAIL_PROVIDER_FROM="Acme Store <orders@example.com>"

TEST_FORCE_EMAIL_FAILURE=0
TEST_EMAIL_DELAY_MS=0

AUTH_USERS='{"alice":"tok_alice_dev","bob":"tok_bob_dev"}'